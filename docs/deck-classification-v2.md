# 卡组分类 v2

- 必须以核心武将轴为主分类。
- 禁止把共同 Cost 作为主分类规则。
- 必须用共现搭配生成分类名。
- 必须用规则表标注 `号令`、`バランス`、`多枚数`。
- 必须用结构兜底未知类型。
- 必须输出使用率和胜率证据。
- 必须把核心分数接近的卡组标记为 `needsReview`。
- 真实数据只能通过 CLI 参数传入。
- 真实样例：39 卡组，31 分类，15 需复核。
- 真实样例：0 个 `unclassified`。
